// Constants
import { Names } from '@/utils/constants';

// Modules
import { StaticModelModules } from '@/models/modules';

export default class Storages extends StaticModelModules {
  constructor() {
    super(Names.storages);
  }
}
