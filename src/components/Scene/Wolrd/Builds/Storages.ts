// Constants
import { Names } from '@/utils/constants';

// Modules
import { StaticModelsBuilds } from '@/models/modules';

export default class Storages extends StaticModelsBuilds {
  constructor() {
    super(Names.storages);
  }
}
